# Decoded mhtml intermediate text dumps (regenerated by parse_odds.py)
*_text.txt

# Python
__pycache__/
*.pyc
*.pyo

# Workspace artifacts (raw API responses + intermediate JSONs are regenerable)
# but we DO want to commit the analysis outputs (PARLAY_*.md, calibration data)
